spring:
  application:
    name: docs-assistant

server:
  port: 8080

# Comma separated list of origins allowed to call /api/*
app:
  cors:
    allowed-origins: http://localhost:5173

management:
  endpoints:
    web:
      exposure:
        exclude: health,info